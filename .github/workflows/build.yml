name: Build & Publish
on:
  workflow_dispatch:
  push:  # 临时加这行，让 GitHub 识别
    branches: [ch347]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt update
          sudo dpkg --add-architecture i386
          sudo apt update
          sudo apt install -y mingw-w64
          sudo apt install -y linux-libc-dev
          sudo apt install -y gcc make libudev-dev
          sudo apt install -y make wget gcc g++
          sudo apt install -y gcc-multilib g++-multilib libc6-dev-i386
          sudo apt install -y gcc-arm-linux-gnueabihf g++-arm-linux-gnueabihf
          sudo apt install -y gcc-aarch64-linux-gnu g++-aarch64-linux-gnu
          sudo apt install -y libc6-dev-i386 lib32gcc-12-dev

      - name: Build Windows
        run: |
          chmod +x build-for-windows.sh
          ./build-for-windows.sh

      - name: Build Linux
        run: |
          chmod +x build-for-linux.sh
          ./build-for-linux.sh

      - name: Show files
        run: ls -lhR build/

      - name: Zip build folder
        run: zip -r build-all.zip build/

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: build-${{ github.run_number }}
          files: build-all.zip
          overwrite: true
